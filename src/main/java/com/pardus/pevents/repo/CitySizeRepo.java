package com.pardus.pevents.repo;

import com.pardus.pevents.model.CitySize;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CitySizeRepo extends JpaRepository<CitySize, Long> {
}
